apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Test Approval
on:
  workflow_dispatch:
    inputs:
      input1:
        type: string
        default: Parameter default
        required: true
        description: Parameter description
      input2:
        type: boolean
        default: true
        required: true
jobs:
  build:
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
  prebuild:
    delegates: cloudbees-io/manual-approval/custom-job.yml@v1
    with:
      approvers: []
      disallowLaunchByUser: false
      instruction: Please approve so build can be done
